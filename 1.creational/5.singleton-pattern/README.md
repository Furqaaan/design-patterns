The Singleton Pattern ensures that only one instance of a class exists in the whole application, and it provides a global point of access to it.

When to use Singleton
-Database connection pool
-Logger class
-Configuration manager
-Cache system (like Redis client)